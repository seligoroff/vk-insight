# Аналитика эффективности постов VK

Команда `vk:analytics` предоставляет углубленный анализ эффективности постов группы или пользователя VK. Она рассчитывает метрики вовлеченности, определяет оптимальное время публикации, находит лучшие посты и сравнивает периоды.

## 📐 Формула Engagement Rate (ER)

**Engagement Rate (ER)** - это процент вовлеченности аудитории, показывающий, какая доля подписчиков взаимодействует с контентом.

**Формула расчета:**
```
ER = (лайки + репосты + комментарии) / количество_подписчиков × 100%
```

**Пример расчета:**
- Пост получил: 150 лайков, 10 репостов, 5 комментариев
- У группы: 5000 подписчиков
- ER = (150 + 10 + 5) / 5000 × 100% = 165 / 5000 × 100% = **3.3%**

**Важно:**
- ER рассчитывается для каждого поста отдельно
- Средний ER - это среднее арифметическое ER всех постов за период
- Если количество подписчиков неизвестно (не удалось получить из API), ER рассчитывается без учета подписчиков и показывается как "N/A"

**⚠️ Ограничение точности расчета ER:**
- Для расчета ER используется **текущее** количество подписчиков группы на момент выполнения команды
- Это означает, что ER для старых постов может быть **неточным**, так как количество подписчиков меняется со временем
- Например: если пост был опубликован год назад, когда у группы было 5000 подписчиков, а сейчас их 7000, то ER будет рассчитан с использованием 7000, что занизит реальный ER того времени
- **Рекомендация:** Для более точного анализа используйте относительно свежие периоды (последние 1-3 месяца), где изменение количества подписчиков минимально

## 🚀 Quick Start

### Шаг 1: Загрузка данных

Перед использованием аналитики необходимо загрузить посты в базу данных:

```bash
# Загрузить посты за последний месяц
php artisan vk:posts-get --owner=-12345678 --from=2024-01-01 --db

# Или за произвольный период
php artisan vk:posts-get --owner=-12345678 --from=2023-01-01 --to=2023-12-31 --db
```

### Шаг 2: Базовый анализ

```bash
# Простой анализ за месяц
php artisan vk:analytics --owner=-12345678
```

Команда выведет:
- Общую статистику (средние лайки, репосты, комментарии, ER)
- ER по дням недели
- Топ-10 постов по всем метрикам

### Шаг 3: Расширенный анализ

```bash
# Анализ с лучшим временем публикации и сравнением периодов
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --best-time \
  --compare=previous \
  --top=5
```

## 📊 Что анализируется

### 1. Общая статистика

Показывает средние значения метрик за период:
- **Всего постов** - количество постов за период
- **Средние лайки** - среднее количество лайков на пост
- **Средние репосты** - среднее количество репостов на пост
- **Средние комментарии** - среднее количество комментариев на пост
- **Средний ER** - средний Engagement Rate (вовлеченность в процентах)
- **Общая вовлеченность** - сумма всех реакций

**Пример вывода:**
```
=== Общая статистика ===
Всего постов: 45
Средние лайки: 125.5
Средние репосты: 8.2
Средние комментарии: 12.3
Средний ER: 2.5%
```

### 2. ER по дням недели

Показывает, в какие дни недели посты получают больше вовлеченности. Это помогает планировать публикации.

**Пример вывода:**
```
=== ER по дням недели ===
┌─────────────┬─────────┬─────────────┬──────────────┬───────────────┬──────────────────┐
│ День недели │ Постов  │ Средний ER  │ Средние лайки│ Средние репосты│ Средние комментарии│
├─────────────┼─────────┼─────────────┼──────────────┼───────────────┼──────────────────┤
│ Понедельник │    8    │    2.8%     │    150.0     │     10.0      │      15.0        │
│ Вторник     │    7    │    2.5%     │    120.0     │      8.0      │      12.0        │
│ ...
```

### 3. Лучшее время публикации

Анализирует посты по часам и определяет оптимальное время для публикации.

**Почему минимум 3 поста?**
Если в 15:00 был опубликован всего 1 пост с ER 8%, это не значит, что 15:00 - лучшее время. Это может быть случайностью. Для надежных выводов нужно минимум 3 поста в одно и то же время. Поэтому учитываются только часы с минимум 3 постами для статистической значимости.

**Использование:**
```bash
php artisan vk:analytics --owner=-12345678 --best-time
```

**Пример вывода:**
```
=== Лучшее время публикации ===
┌───────┬─────────┬─────────────┬──────────────┐
│ Час   │ Постов  │ Средний ER  │ Рекомендация │
├───────┼─────────┼─────────────┼──────────────┤
│ 14:00 │    12   │    3.2%     │ ⭐ Лучшее    │
│ 18:00 │     8   │    2.8%     │ ⭐ Хорошее   │
│ 10:00 │     5   │    2.1%     │ ⭐ Хорошее   │
│ 20:00 │     6   │    1.8%     │ ⚠️ Среднее   │
```

**Маркировка:**
- ⭐ **Лучшее** - топ-1 час по среднему ER
- ⭐ **Хорошее** - топ-2-3 часа
- ⚠️ **Среднее** - остальные часы

### 4. Топ-посты

Показывает лучшие посты по выбранным метрикам. Можно выбрать несколько метрик одновременно.

**Использование:**
```bash
# Топ-5 постов по ER
php artisan vk:analytics --owner=-12345678 --top=5 --metrics=er

# Топ-10 постов по лайкам и репостам
php artisan vk:analytics --owner=-12345678 --top=10 --metrics=likes,reposts
```

**Пример вывода:**
```
=== Топ-посты по ER ===
┌──────┬───────┬────────┬─────────┬─────────────┬──────────────┬────────────────────┐
│ Ранг │  ER   │ Лайки  │ Репосты │ Комментарии │     Дата     │       Текст        │
├──────┼───────┼────────┼─────────┼─────────────┼──────────────┼────────────────────┤
│  1   │ 10.6% │  500   │   20    │     10      │ 2024-01-15   │ Важный анонс...   │
│  2   │  8.2% │  400   │   15    │      8      │ 2024-01-10   │ Новости театра... │
```

### 5. Сравнение периодов

Сравнивает текущий период с предыдущим и показывает динамику изменений.

**Использование:**
```bash
php artisan vk:analytics --owner=-12345678 --period=month --compare=previous
```

**Пример вывода:**
```
=== Сравнение с предыдущим периодом ===
Текущий период: 2024-01-01 - 2024-01-31
Предыдущий период: 2023-12-01 - 2023-12-31

┌─────────────────────┬──────────────────┬──────────────────────┬──────────────────┐
│      Метрика        │ Текущий период   │ Предыдущий период   │    Изменение     │
├─────────────────────┼──────────────────┼──────────────────────┼──────────────────┤
│ Средний ER (%)      │      2.5%        │       2.1%          │  +19.05% ⬆️      │
│ Средние лайки       │     125.5        │      98.0           │  +28.06% ⬆️      │
│ Средние репосты     │       8.2        │       7.0           │  +17.14% ⬆️      │
│ Средние комментарии │      12.3        │      13.0           │   -5.38% ⬇️      │
│ Количество постов   │       45         │       38            │  +18.42% ⬆️      │
```

**Тренды:**
- ⬆️ **Рост** - изменение более +5%
- ➡️ **Стабильно** - изменение от -5% до +5%
- ⬇️ **Падение** - изменение менее -5%

## 📝 Параметры команды

### Обязательные параметры

- `--owner` - ID владельца стены (обязательный)
  - Для групп: отрицательное число (например, `-12345678`)
  - Для пользователей: положительное число (например, `12345678`)

### Периоды анализа

- `--period` - Период анализа (по умолчанию `month`)
  - `week` - последние 7 дней
  - `month` - последние 30 дней
  - `quarter` - последние 90 дней
  - `year` - последние 365 дней
  - `YYYY-MM-DD:YYYY-MM-DD` - произвольный диапазон (например, `2024-01-01:2024-01-31`)

### Метрики и фильтры

- `--metrics` - Метрики для отображения (по умолчанию `all`)
  - `er` - Engagement Rate (вовлеченность)
  - `likes` - лайки
  - `reposts` - репосты
  - `comments` - комментарии
  - `all` - все метрики
  - Можно указать несколько через запятую: `er,likes`

- `--min-engagement` - Минимальная вовлеченность для учета поста (по умолчанию `0`)
  - Учитывается сумма: лайки + репосты + комментарии
  - Пример: `--min-engagement=10` покажет только посты с 10+ реакциями
  
  **Пример использования:**
  ```bash
  # Без фильтрации
  php artisan vk:analytics --owner=-12345678 --period=month
  # Всего постов: 100
  # Средний ER: 1.2%
  
  # С фильтрацией по минимальной вовлеченности
  php artisan vk:analytics --owner=-12345678 --period=month --min-engagement=50
  # Всего постов: 15 (из 100)
  # Средний ER: 4.5%
  # ⚠️ Внимание: Учитываются только посты с минимум 50 реакциями
  ```

- `--top` - Количество топ-постов (по умолчанию `10`, `0` для отключения)

### Дополнительные опции

- `--best-time` - Показать лучшее время публикации (анализ по часам)
- `--compare=previous` - Сравнить с предыдущим периодом
- `--timezone` - Таймзона для анализа времени
  - Приоритет: опция команды > `VK_ANALYTICS_TIMEZONE` из `.env` > `APP_TIMEZONE` из `.env` > UTC
  - Пример: `--timezone=Europe/Moscow`
  - Рекомендуется указать `VK_ANALYTICS_TIMEZONE=Europe/Moscow` в `.env` для российских групп
  
  **Влияние таймзоны:**
  Таймзона влияет на:
  - Группировку по часам (для `--best-time`)
  - Определение дня недели для постов
  - Вывод дат в отчетах
  
  **Пример:** Пост опубликован в 23:00 UTC
  - Для `Europe/Moscow` (UTC+3) это 02:00 следующего дня
  - Группировка по часам и дню недели изменится соответственно

### Форматы вывода

- `--format` - Формат вывода (по умолчанию `table`)
  - `table` - табличный формат в консоли
  - `json` - структурированный JSON
  - `csv` - CSV файлы (только с `--output`)

- `--output` - Путь к файлу для сохранения результатов
  - Для JSON: создается один файл
  - Для CSV: создается несколько файлов в указанной директории
  - Для table: если указан путь с расширением `.md` или `.markdown`, автоматически сохраняется в Markdown формате
  - Пример: `--output=report.md` создаст Markdown файл с таблицами

## 💡 Примеры использования

### Базовые сценарии

#### Еженедельный отчет

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=week \
  --best-time \
  --top=5
```

#### Месячный анализ с сравнением

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --compare=previous \
  --format=json \
  --output=monthly_report.json
```

#### Анализ за произвольный период

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=2023-01-01:2023-12-31 \
  --top=10 \
  --metrics=all
```

### Продвинутые сценарии

#### Анализ только популярных постов

```bash
# Анализ постов с минимум 50 реакциями
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --min-engagement=50 \
  --top=10
```

**Результат:**
- Показывает только посты с высокой вовлеченностью
- Средний ER будет выше, так как учитываются только успешные посты
- Полезно для анализа "лучших практик"

#### Экспорт в CSV для Excel

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --best-time \
  --compare=previous \
  --format=csv \
  --output=reports/january_2024.csv
```

Создаст файлы:
- `reports/january_2024_summary.csv` - сводная статистика
- `reports/january_2024_by_day.csv` - ER по дням недели
- `reports/january_2024_best_time.csv` - лучшее время
- `reports/january_2024_top_posts_er.csv` - топ-посты по ER
- `reports/january_2024_comparison.csv` - сравнение периодов

#### Экспорт в Markdown

```bash
# Автоматически определяется по расширению .md
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --best-time \
  --output=reports/monthly_report.md
```

Создаст один Markdown файл со всеми данными в виде таблиц. Удобно для:
- Документирования результатов
- Публикации в GitHub/GitLab
- Создания отчетов для руководства

#### Автоматический еженедельный отчет

```bash
# Можно добавить в cron для автоматизации
php artisan vk:analytics \
  --owner=-12345678 \
  --period=week \
  --compare=previous \
  --format=json \
  --output=/path/to/weekly_report_$(date +\%Y\%m\%d).json
```

**Полная автоматизация через cron:**

1. Создайте скрипт `weekly_analytics.sh`:
```bash
#!/bin/bash
cd /path/to/vk-utils

# Обновить данные за последнюю неделю
php artisan vk:posts-get \
  --owner=-12345678 \
  --from="7 days ago" \
  --db \
  --update

# Сгенерировать отчет
php artisan vk:analytics \
  --owner=-12345678 \
  --period=week \
  --compare=previous \
  --best-time \
  --format=json \
  --output=/path/to/reports/weekly_$(date +\%Y\%m\%d).json
```

2. Сделайте скрипт исполняемым:
```bash
chmod +x weekly_analytics.sh
```

3. Добавьте в crontab (каждый понедельник в 9:00):
```bash
0 9 * * 1 /path/to/weekly_analytics.sh
```

**Отправка отчета по email:**
```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=week \
  --format=json \
  --output=/tmp/report.json

# Отправить через mail
cat /tmp/report.json | mail -s "Weekly Analytics Report" manager@example.com
```

#### Анализ с учетом таймзоны

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --best-time \
  --timezone=Europe/Moscow
```

### Анализ конкретных метрик

#### Только Engagement Rate

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --metrics=er \
  --top=10
```

#### Лайки и репосты

```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --period=month \
  --metrics=likes,reposts \
  --top=5
```

## 📈 Интерпретация результатов

### Engagement Rate (ER)

**Формула расчета:**
```
ER = (лайки + репосты + комментарии) / количество_подписчиков × 100%
```

**Детали расчета:**
- Формула применяется к каждому посту отдельно
- Средний ER - это среднее арифметическое ER всех постов за период
- Если количество подписчиков неизвестно, ER показывается как "N/A"

**⚠️ Ограничение точности расчета ER:**
- Для расчета ER используется **текущее** количество подписчиков на момент выполнения команды
- Для старых постов (особенно более 3-6 месяцев назад) ER может быть **неточным**, так как количество подписчиков меняется со временем
- **Пример:** Пост был опубликован год назад, когда у группы было 5000 подписчиков, а сейчас их 7000. ER будет рассчитан с использованием 7000, что занизит реальный ER того времени
- **Рекомендация:** Для более точного анализа используйте относительно свежие периоды (последние 1-3 месяца), где изменение количества подписчиков минимально

**Оценка результатов:**
- **Отлично:** > 5% - очень высокая вовлеченность
- **Хорошо:** 2-5% - нормальная вовлеченность для большинства групп
- **Средне:** 1-2% - ниже среднего, есть потенциал для роста
- **Низко:** < 1% - низкая вовлеченность, требуется анализ контента

### ER по дням недели

Используйте эту информацию для планирования публикаций:
- Публикуйте больше контента в дни с высоким ER
- Экспериментируйте с контентом в дни с низким ER

### Лучшее время публикации

- ⭐ **Лучшее** - оптимальное время для важных анонсов
- ⭐ **Хорошее** - подходит для регулярных публикаций
- ⚠️ **Среднее** - используйте для менее важного контента

**Важно:** Учитываются только часы с минимум 3 постами для статистической значимости.

### Сравнение периодов

- ⬆️ **Рост** - метрика улучшилась, продолжайте в том же духе
- ➡️ **Стабильно** - метрика не изменилась, можно экспериментировать
- ⬇️ **Падение** - метрика ухудшилась, требуется анализ причин

## 🔧 Технические детали

### Требования

- Данные должны быть загружены в таблицу `vk_posts` командой `vk:posts-get --db`
- Миграции должны быть выполнены: `php artisan migrate`
- Токен VK API должен быть настроен в `.env`

### Кеширование

- Количество подписчиков (`members_count`) кешируется на **1 час**
- Это оптимизирует запросы к API при частом использовании команды

### Производительность

- Для больших периодов (year, quarter) или больших объемов (>1000 постов) показывается progress bar
- Составной индекс `(owner_id, date)` оптимизирует запросы к БД

**Примерное время выполнения:**
- 100 постов: < 1 сек
- 1000 постов: ~3-5 сек
- 10000 постов: ~30-60 сек (показывается progress bar)

**Оптимизация:**
- Кеш `members_count` экономит ~0.5 сек на каждый запуск
- Составной индекс ускоряет запросы к БД в 5-10 раз для больших объемов

### Форматы вывода

#### JSON формат

Структурированный JSON со всеми данными:
```json
{
  "owner_id": "-12345678",
  "period": {
    "from": "2024-01-01",
    "to": "2024-01-31"
  },
  "members_count": 5000,
  "summary": {
    "total_posts": 45,
    "avg_likes": 125.5,
    "avg_reposts": 8.2,
    "avg_comments": 12.3,
    "avg_er": 2.5,
    "total_engagement": 6565
  },
  "er_by_day": [
    {
      "day_of_week": 1,
      "day_name": "Понедельник",
      "posts_count": 8,
      "avg_er": 2.8,
      "avg_likes": 150.0,
      "avg_reposts": 10.0,
      "avg_comments": 15.0
    },
    {
      "day_of_week": 2,
      "day_name": "Вторник",
      "posts_count": 7,
      "avg_er": 2.5,
      "avg_likes": 120.0,
      "avg_reposts": 8.0,
      "avg_comments": 12.0
    }
  ],
  "best_time": [
    {
      "hour": 14,
      "posts_count": 12,
      "avg_er": 3.2,
      "avg_likes": 200.0,
      "avg_reposts": 12.0,
      "avg_comments": 18.0
    },
    {
      "hour": 18,
      "posts_count": 8,
      "avg_er": 2.8,
      "avg_likes": 180.0,
      "avg_reposts": 10.0,
      "avg_comments": 15.0
    }
  ],
  "top_posts": {
    "er": [
      {
        "post_id": 15342,
        "date": "2024-01-15 16:12:22",
        "text": "Важный анонс...",
        "likes": 500,
        "reposts": 20,
        "comments": 10,
        "er": 10.6,
        "url": "https://vk.com/wall-12345678_15342"
      }
    ],
    "likes": [
      {
        "post_id": 15342,
        "date": "2024-01-15 16:12:22",
        "text": "Важный анонс...",
        "likes": 500,
        "reposts": 20,
        "comments": 10,
        "er": 10.6,
        "url": "https://vk.com/wall-12345678_15342"
      }
    ]
  },
  "comparison": {
    "current": {
      "avg_er": 2.5,
      "avg_likes": 125.5,
      "avg_reposts": 8.2,
      "avg_comments": 12.3,
      "total_posts": 45
    },
    "previous": {
      "avg_er": 2.1,
      "avg_likes": 98.0,
      "avg_reposts": 7.0,
      "avg_comments": 13.0,
      "total_posts": 38
    },
    "changes": {
      "avg_er": {
        "percent": 19.05,
        "trend": "⬆️"
      },
      "avg_likes": {
        "percent": 28.06,
        "trend": "⬆️"
      }
    },
    "periods": {
      "current": {
        "from": "2024-01-01",
        "to": "2024-01-31"
      },
      "previous": {
        "from": "2023-12-01",
        "to": "2023-12-31"
      }
    }
  }
}
```

#### CSV формат

При использовании `--output` создается несколько CSV файлов:
- `analytics_summary.csv` - сводная статистика
- `analytics_by_day.csv` - ER по дням недели
- `analytics_best_time.csv` - лучшее время (если `--best-time`)
- `analytics_top_posts_{metric}.csv` - топ-посты по каждой метрике
- `analytics_comparison.csv` - сравнение (если `--compare=previous`)

Все CSV файлы содержат BOM для корректного отображения кириллицы в Excel.

## ❓ Часто задаваемые вопросы

### Почему команда не находит посты?

Убедитесь, что:
1. Данные загружены командой `vk:posts-get --db`
2. Период анализа соответствует периоду загруженных данных
3. Указан правильный `--owner`

### Как обновить данные для анализа?

```bash
# Обновить существующие записи
php artisan vk:posts-get --owner=-12345678 --from=2024-01-01 --db --update

# Или загрузить заново
php artisan vk:posts-get --owner=-12345678 --from=2024-01-01 --db --clear
```

### Почему не показывается лучшее время?

Лучшее время показывается только если:
- Указана опция `--best-time`
- Есть часы с минимум 3 постами (для статистической значимости)

### Как интерпретировать отрицательные изменения в сравнении?

Отрицательные изменения (⬇️) не всегда плохо:
- Если падает количество постов, но растет ER - это может быть оптимизация контента
- Если падает ER, но растет количество постов - возможно, нужно улучшить качество

### Можно ли анализировать несколько групп одновременно?

Да, но для каждой группы нужно:
1. Загрузить данные отдельно: `vk:posts-get --owner=-11111111 --db`
2. Запустить аналитику отдельно: `vk:analytics --owner=-11111111`

Данные разных групп хранятся в одной таблице и не смешиваются благодаря `owner_id`.

**Пример сравнения нескольких групп:**
```bash
# Анализ первой группы
php artisan vk:analytics \
  --owner=-11111111 \
  --period=month \
  --format=json \
  --output=reports/group1_monthly.json

# Анализ второй группы
php artisan vk:analytics \
  --owner=-22222222 \
  --period=month \
  --format=json \
  --output=reports/group2_monthly.json

# Анализ третьей группы
php artisan vk:analytics \
  --owner=-33333333 \
  --period=month \
  --format=json \
  --output=reports/group3_monthly.json

# Можно написать простой скрипт для сравнения JSON файлов
```

### Команда выводит "Посты за указанный период не найдены"

**Причины:**
1. В указанный период нет постов в БД
2. Все посты отфильтрованы параметром `--min-engagement`
3. Неверно указан `--owner`

**Решение:**
1. Проверьте период: `php artisan vk:posts-get --owner=-12345678 --from=2024-01-01 --db`
2. Уменьшите `--min-engagement` или уберите фильтр
3. Убедитесь, что `owner_id` правильный (для групп отрицательный)

## 🤖 Автоматизация

### Еженедельный отчет через cron

См. пример в разделе [Автоматический еженедельный отчет](#автоматический-еженедельный-отчет).

### Webhook уведомления

```bash
# После анализа отправить данные в Slack/Telegram
php artisan vk:analytics \
  --owner=-12345678 \
  --format=json \
  --output=/tmp/report.json

# Отправить в Slack
curl -X POST https://hooks.slack.com/services/YOUR/WEBHOOK/URL \
  -H 'Content-Type: application/json' \
  -d @/tmp/report.json
```

### Интеграция с Google Sheets

Можно автоматически загружать данные в Google Sheets для совместной работы команды. Используйте CSV формат и Google Sheets API для автоматической загрузки.

## 📊 Визуализация результатов

Хотя команда не генерирует графики, вы можете визуализировать данные:

### Импорт CSV в Excel/Google Sheets

1. Экспортируйте данные в CSV:
```bash
php artisan vk:analytics \
  --owner=-12345678 \
  --format=csv \
  --output=reports/analytics.csv
```

2. Откройте файлы в Excel или Google Sheets
3. Постройте графики используя встроенные инструменты

### Использование Python для визуализации

```python
import json
import matplotlib.pyplot as plt

# Загрузить данные
with open('analytics.json') as f:
    data = json.load(f)

# График ER по дням недели
days = [d['day_name'] for d in data['er_by_day']]
ers = [d['avg_er'] for d in data['er_by_day']]

plt.bar(days, ers)
plt.title('ER по дням недели')
plt.xlabel('День недели')
plt.ylabel('ER (%)')
plt.xticks(rotation=45)
plt.tight_layout()
plt.savefig('er_by_day.png')

# График лучшего времени публикации
hours = [f"{h['hour']}:00" for h in data['best_time']]
hour_ers = [h['avg_er'] for h in data['best_time']]

plt.figure()
plt.plot(hours, hour_ers, marker='o')
plt.title('Средний ER по часам')
plt.xlabel('Час')
plt.ylabel('ER (%)')
plt.xticks(rotation=45)
plt.grid(True)
plt.tight_layout()
plt.savefig('best_time.png')
```

### Использование JavaScript (Chart.js)

```javascript
// Загрузить JSON данные
fetch('analytics.json')
  .then(response => response.json())
  .then(data => {
    // График ER по дням недели
    const ctx = document.getElementById('erChart').getContext('2d');
    new Chart(ctx, {
      type: 'bar',
      data: {
        labels: data.er_by_day.map(d => d.day_name),
        datasets: [{
          label: 'ER (%)',
          data: data.er_by_day.map(d => d.avg_er),
          backgroundColor: 'rgba(54, 162, 235, 0.5)'
        }]
      },
      options: {
        scales: {
          y: {
            beginAtZero: true
          }
        }
      }
    });
  });
```

## 📈 Бенчмарки по индустриям

Для оценки вашего ER сравните с средними показателями:

| Индустрия | Средний ER | Комментарий |
|-----------|------------|-------------|
| E-commerce | 1.5-2.5% | Низкая вовлеченность из-за коммерческого характера |
| Образование | 3-5% | Высокая вовлеченность благодаря образовательному контенту |
| Развлечения | 5-8% | Очень высокая вовлеченность, эмоциональный контент |
| Бизнес B2B | 1-2% | Низкая вовлеченность, профессиональная аудитория |
| Личный бренд | 2-4% | Средняя вовлеченность, зависит от популярности |
| Культура/Искусство | 2-4% | Средняя вовлеченность, эмоциональный контент |

**Важно понимать:**
- Небольшие группы (< 10K подписчиков) обычно имеют **выше** ER
- Крупные группы (> 100K подписчиков) имеют **ниже** ER
- Ваш ER может отличаться от среднего - это нормально, важно отслеживать динамику

**Как использовать бенчмарки:**
- Сравнивайте свой ER с показателями вашей индустрии
- Отслеживайте динамику: рост ER важнее абсолютных значений
- Учитывайте размер аудитории при сравнении

## 🔗 Связанные команды

- [`vk:posts-get`](commands.md#vkposts-get---получение-постов-за-период) - загрузка постов в БД
- [`vk:check`](commands.md#vkcheck---проверка-последних-постов-в-группах) - быстрая проверка последних постов
- [`vk:groups-info`](commands.md#vkgroups-info---информация-о-группах) - информация о группах

## 📚 Дополнительные ресурсы

- [Полный список команд](commands.md)
- [Примеры использования](examples.md)
- [Настройка проекта](configuration.md)

